{
  "name": "@lyjwpage/api",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Telemetry ingest, SQLite state and browser live push on Cloudflare Workers and Durable Objects",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types --import ../../src/lib/testing/register-alias.mjs ../../src/lib/sqlite-store.test.mjs src/*.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.109.2"
  }
}
